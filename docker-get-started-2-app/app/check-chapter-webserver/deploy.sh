#!/bin/bash

# Ajouter les fichiers et dossiers au repository Git
git add .

# Effectuer le commit avec un message
git commit -m "Mise à jour du script check-chapter et Dockerfile"

# Pousser les modifications vers le repository distant (remplacez 'origin' et 'main' par vos valeurs si elles sont différentes)
git push origin main